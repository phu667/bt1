#include <iostream>
using namespace std;

int main() {
    int a, b;
    cout << "Nhap hai so nguyen a va b: ";
    cin >> a >> b;
    int max_val = (a > b) ? a : b;
    cout << "Gia tri lon nhat la: " << max_val << endl;
    return 0;
}
