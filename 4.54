#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cout << "Nhap 3 so a, b, c: ";
    cin >> a >> b >> c;

    int maxVal;

    if (a >= b && a >= c) {
        maxVal = a;
    } else if (b >= a && b >= c) {
        maxVal = b;
    } else {
        maxVal = c;
    }

    cout << "Gia tri lon nhat la: " << maxVal << endl;
    return 0;
}
